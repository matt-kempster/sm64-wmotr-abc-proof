# Rank-1 six-residual audit

## Result

No new clean collision/query counterexample was found. The read-only retail receipt now extends continuously from timer 348 through timer 2810 along an independently constructed original-JP route that touches all four pillars and enters the upper warp with zero A input. All 2,462 frame checks pass: every protected write, allocator change, list, owner, outside destination, and final Mario platform query satisfies the invariant; all 149,578 `find_floor` entries have matching checked returns; and all 426 dynamic-floor returns observe their installed live owners. Mario's final platform query selects an ownerless static floor in every audited frame, so the route never caches the pyramid top. The only apparent lifecycle failure occurs when the top explodes: its behavior deactivates it, its script nevertheless installs six triangles, and unloading removes the owner later that frame. Return-side receipts show that no query returns any dynamic floor in that frame, the following frame clears the six triangles before every query, and the refined pending-clear rule is therefore safe. The route reaches the upper-warp action at timer 2808 and Area 2 at timer 2830 with all three A counters zero, but produces no useful positive Object/State/Graphics split. This closes the six residuals for one real successful upper-warp execution; a universal theorem over every controller history remains open. The checked bundles now include `Area1Rank1UpperWarpTraceCheckedBoundary`.

## Outcome by possibility

| Possibility | Outcome | Counterexample value |
| --- | --- | --- |
| Wrong live `gCurrentObject` during surface installation | Ruled out for the complete successful upper-warp trace. Every ordinary moving-floor return has the exact owner installed by the live current object. The top's six post-deactivation stores are isolated as pending cleanup and are never returned by a query. | No producer in this execution. |
| Pre-existing or type-punned alias | The accepted JP heads show that the surface pool is the last live left allocation. Full cached/uncached range watches cover every one of the 2,462 frames and report no overlapping or misclassified write. | Closed for this execution; a different history still needs the same invariant. |
| Outside call with independent surface access | All reached narrowed roots and their destinations are counted continuously, including camera, object, audio, sound-stop, puzzle-jingle, and store-free `sqrtf` paths. No protected-destination failure occurs. | Closed for this execution. |
| Stale surface/object across a lifetime boundary | A stale cached **object** remains a real downstream carrier. The top explosion also creates a real six-triangle stale-surface interval, but no dynamic floor is returned during that interval and the next clear precedes every query. | The newly observed stale-surface interval is inert on this route. |
| Legitimate geometry or scheduling selects another inserted floor | All 149,578 floor returns are tied to intact lists, and every one of Mario's 2,462 final platform selections is static and ownerless. The upper-warp frame does not select the top. | No unexpected selected platform in this execution. |
| Runtime pointer mutation/forwarding creates another PLAYER node | Every end-of-frame object-list scan passes through the upper warp, and no owner/list/slot failure occurs. | No stock producer in this execution. |

## Wrong current object

The whole US/JP source census finds only three direct writers of `gCurrentObject`: the normal traversal, the time-stop traversal, and deactivated-object unloading. None occurs in the closed direct-call graph of the canonical Area-1 surface owners. That graph contains exactly one indirect call site, `cur_obj_call_action_function`. The only stock arrays used by the relevant Tox Box and exclamation-box loops contain fourteen exact action targets in each version; none is a current-object writer, and no internal source assignment changes either array. This closes ordinary callback re-entry. Because the arrays are public and writable, a linked run must still preserve their bytes and establish the initial object selected by traversal.

## Aliases and shared main-pool storage

The accepted JP receipt now fixes the node payload at `[0x80182B20,0x801905E0)`, the surface payload at `[0x801905F0,0x801AB530)`, and the live allocator heads at `left=0x801AB530` and `right=0x801C0FF0`, with 88,752 bytes free. The surface payload is therefore exactly the last live left allocation. `Area1SurfacePoolRangeSeparation.v` checks all eleven generated callers of `main_pool_alloc`, all writers of the allocator heads/state, and the complete uses of the two public surface-pool globals; no code takes either global's address. Its live-epoch relation proves that any later fitting left or right allocation remains above the surface payload and that a safe saved-state restoration cannot rewind across it. At CompCert byte level, a store through any differently typed pointer preserves both pools unless its actual same-block byte interval overlaps one of them; if an access owned by another logical allocation fails, that allocation itself overlaps the protected hull. Thus “the pointers are public and share a block” is no longer a counterexample. The remaining alias cases are concrete: a live cross-allocation pointer computation, a pop/free/reallocation below the surface endpoint, or a descriptor retargeted to an overlapping address.

## Outside calls

The direct JP roots are no longer based on a manual source impression. The new hash-gated receipt authenticates every instruction in `set_camera_shake_from_point`, `create_sound_spawner`, `cur_obj_play_sound_2`, `play_sound`, and `play_puzzle_jingle`: exactly 163 instructions produce 29 stores and 11 direct calls, with no hidden indirect call. Every direct store is to the game-thread stack, object pool, sound-request storage, or fixed puzzle state, all disjoint from the live surface ranges. The earlier retail certificate proves `sqrtf` store-free and frames `stop_sounds_from_source` and its transitive helpers. For the calls made by the five new roots, the Coq effect relation permits only bounded stack/static/object writes or writes inside the separately linked JP audio heap `[0x801CE000,0x801FF200)`, which is above the main pool. This is an exact protected-region specification, but its live premise still matters: a corrupted audio/object pointer that redirects a callee into `[0x80182B20,0x801AB530)` fails the relation and becomes the first concrete producer. The remaining work is therefore descriptor validity and actual call-trace membership, not an unnamed external with arbitrary memory access.

## Lifetime

The lifetime investigation found both a genuine downstream survivor and a harmless one-frame cleanup interval. The executable model can retain a cached platform pointer to a freed, inactive, unreused slot whose resident bytes still move MarioState, validating Rank 1's later carrier. In the real clean trace, the exploding top first deactivates itself, then its behavior script calls the collision loader and installs six triangles, after which end-of-frame unloading unlinks the owner. Those triangles remain linked until the next frame's dynamic clear. The return-side audit checks all 99 `find_floor` calls in the explosion frame, observes zero dynamic returns, then checks that the next frame clears first and again has zero dynamic returns. Thus the concrete stale surfaces are real but inert; they do not install the missing cached top pointer.

## Alternate floor and PLAYER forwarding

The finite stock query returns null at the upper-warp sample and also for the same X/Z coordinates at checked final-query height at most 896. Therefore scheduling among the modeled inserted stock floors cannot by itself select another owner there. Separately, `bhvMario` is the only generated behavior initially selecting list 0, occurs at one initializer and in no internal function body, while post-copy Mario particles and debug children select non-PLAYER lists. The only direct behavior writers are initial spawn, `create_object`, and two setters; the setters do not relink their objects. The only direct `next`/`prev` writers are allocation, deallocation, and list clearing, and the constructor chain is `create_object -> allocate_object -> try_allocate_object`. All four direct writers of the global list-root pointer copy the canonical `gObjectListArray` value exactly once; `gMarioObject` itself is written only by area spawning and clearing, and its pointer cell is never passed by address. Thus a second PLAYER node now requires a live mutated behavior command/pointer at construction, a direct alias/list-link effect, an outside effect, or a lifetime violation—not ordinary pointer forwarding by named source.

## Continuous baseline-frame receipt

The ROM-hash-gated `jp-rank1-live-boundary` audit begins at `update_objects` on timer 348 and ends at the next entry on timer 349 without writing game memory or pressing A. The frame makes one `alloc_only_pool_init` allocation whose 16-byte header begins exactly at the exclusive surface-pool end `0x801AB530`, whose payload is `[0x801AB540,0x801C0FE0)`, and whose matching `geo_process_root` free restores the left head to `0x801AB530`; the four allocator-global stores are recorded individually, so endpoint equality cannot hide an intervening rewind. Full cached and uncached range watches classify 238 protected-pool stores as authenticated dynamic surface/node construction, 776 dynamic-partition stores as the normal clear and eight list-head insertions, zero static-partition stores, zero surface-pointer-cell stores, and zero unsafe or boundary-overlapping stores. Six exact `gCurrentObject -> Surface.object` stores pair one-for-one with six `add_surface` calls, producing six moving triangles and 24 dynamic nodes; every owner stays active in its original object-list ring with the same behavior, all thirteen object rings are intact, and static and dynamic partitions are ordered, acyclic, unique, and have exact node coverage. The final platform query at `(653,38,6566)` selects `0x80199D70`, surface-pool index 808, exactly once from the static floor list, with null owner and null platform; its recorded words decode to the stock Area-1 `SURFACE_HARD` triangle `(214,225,223)`. Of the narrowed outside roots, only the independently store-free `sqrtf` executes. The exact receipt is checked by `Area1Rank1LiveBoundaryReceipt.v`; it proves this frame, not all later controller histories.

## Continuous four-pillar and upper-warp receipt

Mode 12 turns the supplied 2013 video into an independent, reproducible original-JP execution rather than treating the video's unknown region and unavailable input log as proof data. The controller-only schedule takes the east jumping box to the first pillar, uses the southeast Tweester for the second, performs a B speedkick/dive across the next gap, follows a checked hard-floor path around the Tox Boxes to the two western pillars, waits for the top to explode, takes the west jumping box up the pyramid, and uses a B-only stomach-slide rollout into the upper warp. Pillars increment at timers 848, 1065, 2390, and 2548; the top starts spinning at 2549 and explodes at 2700; the box lands near the top at 2776; the rollout begins at 2782; the upper-warp actions occur at 2807 and 2808; and Area 2 loads at 2830. The result fixes `mode12PillarsComplete=1`, `warpDisappeared=1`, `warpUsedObj=1`, and `aPressedFrames=aDownFrames=controllerAFrames=0`.

The Rank-1 extension audits timers 348 through 2809 inclusive. It checks 2,462 consecutive frames, 149,578 `find_floor` entries and returns, 426 valid dynamic-floor returns, every moving-surface owner/list receipt, and every narrowed outside destination; it reports zero failed frames, zero return-side owner failures, and zero queries before each frame's dynamic clear. Mario's final platform query is ownerless and static in all 2,462 frames. `Area1Rank1UpperWarpTraceReceipt.v` packages these exact finite facts and separately proves the top-explosion interval is pending-clear only.

## What remains to close Rank 1

The requested clean upper-warp extension is complete and passes, so none of the named residuals occurs on this successful four-pillar schedule. Closing Rank 1 for every successful in-bounds execution now requires a universal linked invariant, or equivalent coverage of every materially distinct controller/scheduler history, showing the same allocator, protected-write, outside-destination, owner/list, return-side query, and pending-clear properties. A counterexample must therefore differ from this run at a concrete checked boundary: it must cause an overlapping store, retargeted destination, wrong or stale owner that is actually returned, unexpected final platform, positive useful view split, or a query before clearing. The cached inactive object remains a valid later carrier if some different clean history first installs the top pointer. Machine-only out-of-bounds writes, ACE, DMA, and post-undefined-behavior continuation remain outside the current CompCert verdict.

## Scope

Out-of-bounds pool indexing, forged machine pointers, ACE, DMA, and retail continuation after undefined behavior remain outside this CompCert verdict and require a machine-level extension.
