# Inputs

This project intentionally has no hand-written C model. The Makefile invokes
`clightgen` on the pinned decompile's real `src/game/game_init.c` and
`src/menu/title_screen.c` translation units.

